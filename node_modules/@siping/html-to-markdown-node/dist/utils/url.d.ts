export declare function resolveURL(url: string, domain?: string): string;
export declare function isAbsoluteURL(url: string): boolean;
/**
 * Escape Markdown special characters in URL for use in []() syntax.
 * Only escapes characters that would break Markdown parsing: ( ) [ ]
 */
export declare function escapeMarkdownURL(url: string): string;
//# sourceMappingURL=url.d.ts.map