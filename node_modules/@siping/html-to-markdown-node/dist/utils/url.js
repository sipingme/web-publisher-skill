"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveURL = resolveURL;
exports.isAbsoluteURL = isAbsoluteURL;
exports.escapeMarkdownURL = escapeMarkdownURL;
function resolveURL(url, domain) {
    if (!domain || isAbsoluteURL(url)) {
        return url;
    }
    try {
        const base = new URL(domain);
        const resolved = new URL(url, base);
        return resolved.href;
    }
    catch (e) {
        return url;
    }
}
function isAbsoluteURL(url) {
    try {
        new URL(url);
        return true;
    }
    catch (e) {
        return false;
    }
}
/**
 * Escape Markdown special characters in URL for use in []() syntax.
 * Only escapes characters that would break Markdown parsing: ( ) [ ]
 */
function escapeMarkdownURL(url) {
    return url.replace(/([()[\]])/g, '\\$1');
}
//# sourceMappingURL=url.js.map