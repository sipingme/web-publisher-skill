"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.shouldEscapeChar = shouldEscapeChar;
exports.escapeMarkdown = escapeMarkdown;
exports.unescapeMarkdown = unescapeMarkdown;
const MARKDOWN_SPECIAL_CHARS = new Set([
    '*', '_', '[', ']', '(', ')', '#', '+', '-', '.', '!', '`', '\\', '|', '~', '>', '<'
]);
function shouldEscapeChar(char) {
    return MARKDOWN_SPECIAL_CHARS.has(char);
}
function escapeMarkdown(text, escapeChars) {
    let result = '';
    for (let i = 0; i < text.length; i++) {
        const char = text[i];
        if (escapeChars.has(char)) {
            result += '\\' + char;
        }
        else {
            result += char;
        }
    }
    return result;
}
function unescapeMarkdown(text) {
    return text.replace(/\\([*_\[\]()#+\-.!`\\|~><])/g, '$1');
}
//# sourceMappingURL=escape.js.map