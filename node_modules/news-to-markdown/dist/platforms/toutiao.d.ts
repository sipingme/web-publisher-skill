import { BasePlatform } from './base';
export declare class TouTiaoPlatform extends BasePlatform {
    name: string;
    metaSelectors: {
        title: string;
        author: string;
        time: string;
    };
    detect(url: string): boolean;
    preprocess(html: string, url: string): string;
    /**
     * 移除头条页面底部的推荐文章区域
     * 推荐文章在 article 外的 .item-container 中，包含 "去今日头条看专享资讯" 等链接
     */
    private removeRecommendations;
    private normalizeImages;
    private normalizeHeadings;
    private normalizeBullets;
    private normalizeCodeBlocks;
    private normalizeTables;
    postprocess(markdown: string): string;
}
