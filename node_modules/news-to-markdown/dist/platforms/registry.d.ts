import { Platform } from '../types';
export declare class PlatformRegistry {
    private platforms;
    constructor();
    registerBuiltinPlatforms(): void;
    register(platform: Platform): void;
    get(name: string): Platform;
    detect(url: string): Platform;
    list(): Platform[];
}
