import { BasePlatform } from './base';
/**
 * 36kr 平台适配器
 *
 * 注意：36kr 电脑端页面有反爬机制，需要使用移动端页面
 * 移动端 URL 格式：https://m.36kr.com/p/{article_id}
 * 电脑端 URL 格式：https://36kr.com/p/{article_id}
 */
export declare class Kr36Platform extends BasePlatform {
    name: string;
    metaSelectors: {
        title: string;
        author: string;
        time: string;
    };
    private get noiseSelectors();
    private get contentSelectors();
    detect(url: string): boolean;
    /**
     * 将电脑端 URL 转换为移动端 URL
     * 36kr 电脑端有反爬机制，移动端更容易获取内容
     */
    getMobileUrl(url: string): string;
    preprocess(html: string, url: string): string;
    postprocess(markdown: string): string;
}
