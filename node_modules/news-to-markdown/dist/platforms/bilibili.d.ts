import { BasePlatform } from './base';
export declare class BilibiliPlatform extends BasePlatform {
    name: string;
    metaSelectors: {
        title: string;
        author: string;
        time: string;
    };
    detect(url: string): boolean;
    preprocess(html: string, url: string): string;
    postprocess(markdown: string): string;
    private processBilibiliContent;
}
