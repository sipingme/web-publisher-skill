import { BasePlatform } from './base';
export declare class WechatPlatform extends BasePlatform {
    name: string;
    metaSelectors: {
        title: string;
        author: string;
        time: string;
    };
    postprocess(markdown: string): string;
    private normalizeParagraphBreaksForWechat;
    private ensureBoldNumberedHeadingStandalone;
    private ensurePlainNumberedHeadingStandalone;
    private normalizeSplitSectionHeadings;
    private ensureSectionHeadingStandalone;
    private normalizeCodeBlockCommandLines;
    private normalizeCollapsedYamlLikeLine;
    private normalizeCollapsedEnvLine;
    private isPlainTextLine;
    /**
     * 确保代码块前后有正确的空行格式
     * 修复：
     * ```
     *
     * # 注释
     * ```
     * 变成：
     *
     * ```
     * # 注释
     * ```
     */
    private normalizeCodeBlockSpacing;
    detect(url: string): boolean;
    preprocess(html: string, url: string): string;
    /**
     * 展开嵌套在装饰性容器（如 135编辑器、秀米等）中的图片
     * 这些图片被深层嵌套在 section 中，Readability 可能会过滤掉
     * 策略：只处理纯装饰性容器（有图片但文本很少），用简单的图片标签替换
     */
    private flattenNestedImages;
    private preserveSimpleSectionBoundaries;
    private processWechatStyles;
}
