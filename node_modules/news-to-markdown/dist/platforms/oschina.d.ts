import { BasePlatform } from './base';
export declare class OschinaPlatform extends BasePlatform {
    name: string;
    metaSelectors: {
        title: string;
        author: string;
        time: string;
    };
    detect(url: string): boolean;
    preprocess(html: string, url: string): string;
    private processOschinaContent;
}
