export interface DownloadImageOptions {
    url: string;
    outputDir: string;
    filename?: string;
    timeout?: number;
    maxRetries?: number;
}
export interface DownloadImageResult {
    success: boolean;
    localPath?: string;
    error?: string;
}
/**
 * 下载图片到本地
 */
export declare function downloadImage(options: DownloadImageOptions): Promise<DownloadImageResult>;
/**
 * 批量下载图片
 */
export declare function downloadImages(urls: string[], outputDir: string, verbose?: boolean): Promise<Map<string, string>>;
/**
 * 替换 Markdown 中的图片 URL 为本地路径
 */
export declare function replaceImageUrls(markdown: string, urlToLocalPath: Map<string, string>, baseDir: string): string;
