import { Platform } from '../types';
export interface ExtractedContent {
    title?: string;
    author?: string;
    publishTime?: string;
    content: string;
    contentHtml: string;
    images: string[];
    coverImage?: string;
    textLength: number;
    excerpt?: string;
}
export interface DualExtractorOptions {
    url: string;
    noiseSelectors?: string[];
    preferReadability?: boolean;
    verbose?: boolean;
    platform?: Platform;
    originalHtml?: string;
}
export declare class DualExtractor {
    private verbose;
    constructor(verbose?: boolean);
    extract(html: string, options: DualExtractorOptions): Promise<ExtractedContent>;
    private extractWithReadability;
    /**
     * 直接从微信文章的 #js_content 提取内容
     * Readability 经常截断微信文章内容，这个方法作为备用
     */
    private extractWechatDirectContent;
    private extractWithNewsExtractor;
    /**
     * 判断图片是否为小图标/表情包
     * 微信文章中的表情包通常：
     * 1. URL 包含特定关键词
     * 2. 尺寸很小（< 100px）
     */
    private isSmallEmojiImage;
    private extractAllImages;
    /**
     * 使用平台级 CSS 选择器提取 meta 信息，优先级最高
     */
    private extractWithSelectors;
    private selectText;
    private validateAuthor;
    private selectCoverImage;
    /**
     * 检查图片是否是装饰性图片
     */
    private isDecorativeImage;
    /**
     * 生成动态封面图 URL
     * 使用第三方服务生成基于标题的封面图
     */
    private generateDynamicCover;
}
