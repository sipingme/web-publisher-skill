import { ConvertOptions, ConvertResult } from '../types';
export declare class NewsToMarkdownConverter {
    private platformRegistry;
    private fetcher;
    constructor();
    registerPlatform(platform: any): void;
    convert(options: ConvertOptions): Promise<ConvertResult>;
    private addMetadata;
    /**
     * 移除正文开头与封面相同的重复图片
     * 只移除正文最开始位置的图片（在第一个标题或段落之前）
     */
    private removeDuplicateCoverImage;
    /**
     * 移除正文开头与标题和作者重复的内容
     */
    private removeDuplicateTitleAuthor;
    /**
     * 从 URL 获取扩展名
     */
    private getExtensionFromUrl;
    /**
     * 确保代码块前后有正确的空行格式
     * 移除代码块开始后和结束前的空行
     */
    private normalizeCodeBlockSpacing;
    /**
     * 压缩连续的空行
     * 将 3 个或更多连续空行压缩为 2 个空行
     * 这样可以防止 wenyan-md 生成过多的空 <p> 标签
     */
    private compressEmptyLines;
    /**
     * 清理 Markdown 末尾的参考链接区域
     * 匹配形如 "参考资料:", "来源:", "参考链接:", "Reference:" 等段落及其中的链接
     */
    private cleanTrailingLinks;
}
