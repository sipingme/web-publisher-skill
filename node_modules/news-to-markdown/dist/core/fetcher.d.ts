import { FetchStrategy, FetchResult } from '../types';
export declare class Fetcher {
    private verbose;
    constructor(verbose?: boolean);
    fetch(url: string, strategy?: FetchStrategy, timeout?: number): Promise<FetchResult>;
    private fetchAuto;
    private fetchWithCurl;
    private fetchWithWget;
    /**
     * 头条页面抓取策略：先手机版，无图片则桌面版，都无图片则返回手机版
     */
    private fetchToutiaoWithFallback;
    /**
     * 检查头条页面 article 内是否有图片
     */
    private checkToutiaoImages;
    /**
     * 使用真实 Chrome 浏览器抓取（channel: 'chrome'）
     * 比 Playwright 自带的 Chromium 有更真实的浏览器指纹，可绕过知乎等平台的 zse-ck 反爬
     * 如果 Chrome 未安装，自动降级到普通 Playwright
     */
    private fetchWithRealChrome;
    private fetchWithPlaywright;
}
