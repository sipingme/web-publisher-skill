import { JSDOM } from 'jsdom';
export declare class AuthorExtractor {
    private authorPatterns;
    private authorSelectors;
    extract(dom: JSDOM, authorXPath?: string): string;
    private extractAuthorFromText;
    private cleanAuthorName;
    private evaluateXPath;
}
