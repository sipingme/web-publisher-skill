import { ExtractOptions, NewsContent } from './types';
export declare class NewsExtractor {
    private contentExtractor;
    private titleExtractor;
    private timeExtractor;
    private authorExtractor;
    constructor();
    extract(html: string, options?: ExtractOptions): NewsContent;
    private removeNoiseElements;
    private resolveUrl;
}
export * from './types';
export declare function extractNews(html: string, options?: ExtractOptions): NewsContent;
