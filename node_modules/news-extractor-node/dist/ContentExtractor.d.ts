import { JSDOM } from 'jsdom';
export declare class ContentExtractor {
    private punctuation;
    extract(dom: JSDOM, contentXPath?: string): {
        text: string;
        html: string;
        images: string[];
    };
    private calculateTextDensity;
    private countEffectiveText;
    private extractImages;
    private evaluateXPath;
}
