import { JSDOM } from 'jsdom';
export declare class TimeExtractor {
    private timePatterns;
    private timeSelectors;
    extract(dom: JSDOM, publishTimeXPath?: string): string;
    private extractTimeFromText;
    private normalizeTime;
    private evaluateXPath;
}
